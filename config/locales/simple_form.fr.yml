fr:
  simple_form:
    labels:
      defaults:
        required: "requis"
        optional: "facultatif"
    hints:
      defaults:
        required: "Ce champ est obligatoire"
    errors:
      defaults:
        format: "%{message}"
    placeholders:
      defaults:
        required: "Champ requis"
