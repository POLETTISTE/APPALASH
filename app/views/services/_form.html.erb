<<<<<<< HEAD
<%= simple_form_for(@service) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :price %>
  </div>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
<%# to add service present is website, maybe service.allow_wesite? %>
=======
<div class="container mx-auto px-4 py-8">
  <%= form_for(@service, html: { class: 'space-y-6' }) do |f| %>
    <!-- Error Notification -->
    <%= f.error_notification class: 'text-red-600' %>
    <%= f.error_notification message: f.object.errors[:base].to_sentence, class: 'text-red-600' if f.object.errors[:base].present? %>
    <!-- Form Inputs -->
    <div class="form-inputs space-y-4">
      <div>
        <%= f.label :name, class: 'block text-lg font-medium text-gray-700' %>
        <%= f.text_field :name, class: 'block w-full p-3 border border-gray-300 rounded-lg shadow-sm focus:ring-2 focus:ring-indigo-500' %>
      </div>
      <div>
        <%= f.label :price, class: 'block text-lg font-medium text-gray-700' %>
        <%= f.number_field :price, class: 'block w-full p-3 border border-gray-300 rounded-lg shadow-sm focus:ring-2 focus:ring-indigo-500' %>
      </div>
    </div>
    <!-- Form Actions -->
    <div class="form-actions mt-6">
      <%= f.submit class: 'bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded' %>
    </div>
  <% end %>
</div>
>>>>>>> form
