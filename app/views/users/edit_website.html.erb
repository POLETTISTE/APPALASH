<h1>Edit Profile</h1>
<%= form_for @user, url: update_website_user_profile_path(@user.website), method: :patch, data: { turbo: "true" } do |f| %>
  <!-- Display error messages -->
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>
      <ul>
        <% @user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <!-- Display custom error message if website already exists -->
  <% if flash[:alert] %>
    <div class="alert alert-danger">
      <%= flash[:alert] %>
    </div>
  <% end %>
  <!-- Form fields -->
  <div>
    <%= f.label :website %>
    <%= f.text_field :website %>
  </div>
  <!-- Other fields here -->
  <div>
    <%= f.submit "Update Profile" %>
  </div>
<% end %>
<%= link_to 'Back to Profile', user_profile_path(@user.website), class: 'btn btn-secondary' %>
<%= turbo_stream_from "user_form" %>
