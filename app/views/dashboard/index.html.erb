<h1><%= t('dashboard_h1') %></h1>
  <div id="welcome"></div>

<% if current_user.admin? %>
  <%# Render the admin-specific content %>
  <%= render 'admin_index' %>
<% else %>
  <%# Render the regular user content %>
  <%= render 'non_admin_index' %>
<% end %>
